{% extends '::backend.html.twig' %}

{% block body -%}
    <h1>Docente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Apellidop</th>
                <th>Apellidom</th>
                <th>Email</th>
                <th>Titulo</th>
                <th>Areainteres</th>
                <th>Enlacepagina</th>
                <th>Enlaceimagen</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('docente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.apellidoP }}</td>
                <td>{{ entity.apellidoM }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.titulo }}</td>
                <td>{{ entity.areaInteres }}</td>
                <td>{{ entity.enlacePagina }}</td>
                <td>{{ entity.enlaceImagen }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('docente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('docente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('docente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
