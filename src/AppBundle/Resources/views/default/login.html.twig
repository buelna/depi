{% extends '::backend.html.twig' %}
{% block body %} 
	<div class="row">
		<div class="large_12 columns">

			
		</div>
	</div>
	<div class="row">
		<div class="large-12 columns" align="center">
			<div class="large-6 columns">
				</br>
				<h1>Inicio de sesi&oacuten</h1>
				{% if error %}
				    <div>{{ error.message }}</div>
				{% endif %}

				<form action="{{ path('login_check') }}" method="post" class="forms">
				    <input placeholder="Nombre" type="text" id="username" name="_username" value="{{ last_username }}" />

				    <input type="password" id="password" name="_password" placeholder="Password"  />

				    <button type="submit" class="btn-blue width-100">Iniciar sesi&oacuten</button>
				</form>
			</div>
			<div class="large-6 columns" align="center">
				<div class="row">
					{#<a href="{{ path('usuario_new') }}" class="button expand">Reg&iacutestrate</a>#}
				</div>
			</div>

			<hr>
		</div>
	</div>
{% endblock %}